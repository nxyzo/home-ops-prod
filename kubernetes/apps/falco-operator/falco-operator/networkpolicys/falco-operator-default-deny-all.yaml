apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: falco-operator-default-deny-all
  namespace: falco-operator
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress: []