apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints
  namespace: auth
data:
  passwordless-authentication.yaml: |
    version: 1

    metadata:
      name: Passwordless authentication flow

    entries:
      - model: authentik_flows.flow
        id: passwordless-flow
        identifiers:
          slug: passwordless-authentication-flow
        attrs:
          name: Passwordless Authentication
          title: Login with Passkey
          designation: authentication
          authentication: none
          denied_action: message_continue
          layout: stacked

      - model: authentik_stages_authenticator_validate.authenticatorvalidatestage
        id: passwordless-webauthn-validation
        identifiers:
          name: authentication-webauthn-validation
        attrs:
          not_configured_action: configure
          configuration_stages:
            - !Find [authentik_stages_authenticator_webauthn.authenticatorwebauthnstage, [name, "default-authenticator-webauthn-setup"]]
          device_classes:
            - webauthn
          webauthn_user_verification: required

      - model: authentik_stages_user_login.userloginstage
        id: passwordless-login
        identifiers:
          name: passwordless-authentication-login
        attrs:
          session_duration: seconds=0

      - model: authentik_flows.flowstagebinding
        identifiers:
          target: !KeyOf passwordless-flow
          stage: !KeyOf passwordless-webauthn-validation
          order: 10

      - model: authentik_flows.flowstagebinding
        identifiers:
          target: !KeyOf passwordless-flow
          stage: !KeyOf passwordless-login
          order: 100

      - model: authentik_stages_identification.identificationstage
        identifiers:
          name: default-authentication-identification
        attrs:
          user_fields:
            - username
            - email

          passwordless_flow:
            !Find [
              authentik_flows.flow,
              [slug, passwordless-authentication-flow],
            ]

          webauthn_stage: !KeyOf passwordless-webauthn-validation

  password-policy-hardening.yaml: |
      version: 1
      metadata:
        name: Password policy hardening

      entries:
        - model: authentik_policies_password.passwordpolicy
          id: default-password-change-password-policy
          identifiers:
            name: default-password-change-password-policy
          attrs:
            length_min: 15
            check_static_rules: true
            check_zxcvbn: true
            zxcvbn_score_threshold: 2
            check_have_i_been_pwned: true
            hibp_allowed_count: 0
            password_field: password
            error_message: "Password must be at least 15 characters long and must not appear in known data breaches."
  groups.yaml: |
    version: 1

    metadata:
      name: Groups

    entries:
      - model: authentik_core.group
        id: infra-admins-group
        state: present
        identifiers:
          name: infra-admins
        attrs:
          name: infra-admins
          is_superuser: true
      - model: authentik_core.group
        id: users
        state: present
        identifiers:
          name: users
        attrs:
          name: users
          is_superuser: false

  oidc-longhorn.yaml: |
    version: 1

    metadata:
      name: Longhorn OIDC

    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: longhorn-oidc-provider
        state: present

        identifiers:
          name: Longhorn

        attrs:
          name: Longhorn
          client_id: longhorn-envoy
          client_secret: !Env LONGHORN_OIDC_CLIENT_SECRET
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token

          authorization_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-authorization-implicit-consent]

          invalidation_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-invalidation-flow]

          signing_key: !Find
            - authentik_crypto.certificatekeypair
            - [name, authentik Self-signed Certificate]

          redirect_uris:
            - matching_mode: strict
              url: "https://longhorn.${SECRET_DOMAIN}/oauth2/callback"

          property_mappings:
            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-openid]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-profile]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-email]

      - model: authentik_core.application
        id: longhorn-application
        state: present

        identifiers:
          slug: longhorn

        attrs:
          name: Longhorn
          slug: longhorn
          provider: !KeyOf longhorn-oidc-provider
          meta_launch_url: "https://longhorn.${SECRET_DOMAIN}/"
          meta_icon: "https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/webp/longhorn.webp"
          policy_engine_mode: any
          open_in_new_tab: true
          group: "Infrastructure"
          meta_description: "Cloud-Native distributed storage built on and for Kubernetes"

      - model: authentik_policies.policybinding
        identifiers:
          target: !KeyOf longhorn-application
          group: !Find [authentik_core.group, [name, infra-admins]]
          order: 0
        attrs:
          enabled: true
          negate: false

  oidc-policy-reporter.yaml: |
    version: 1

    metadata:
      name: policy-reporter OIDC

    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: policy-reporter-oidc-provider
        state: present

        identifiers:
          name: policy-reporter

        attrs:
          name: policy-reporter
          client_id: policy-reporter-envoy
          client_secret: !Env POLICY_REPORTER_OIDC_CLIENT_SECRET
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token

          authorization_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-authorization-implicit-consent]

          invalidation_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-invalidation-flow]

          signing_key: !Find
            - authentik_crypto.certificatekeypair
            - [name, authentik Self-signed Certificate]

          redirect_uris:
            - matching_mode: strict
              url: "https://policy-reporter.${SECRET_DOMAIN}/oauth2/callback"

          property_mappings:
            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-openid]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-profile]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-email]

      - model: authentik_core.application
        id: policy-reporter-application
        state: present

        identifiers:
          slug: policy-reporter

        attrs:
          name: Kyverno policy-reporter
          slug: policy-reporter
          provider: !KeyOf policy-reporter-oidc-provider
          meta_launch_url: "https://policy-reporter.${SECRET_DOMAIN}/"
          meta_icon: "https://kyverno.github.io/policy-reporter-docs/logo.png"
          policy_engine_mode: any
          open_in_new_tab: true
          group: "Infrastructure / Security"
          meta_description: "Monitoring and Observability Tool for the PolicyReport CRD with an optional UI."

      - model: authentik_policies.policybinding
        identifiers:
          target: !KeyOf policy-reporter-application
          group: !Find [authentik_core.group, [name, infra-admins]]
          order: 0
        attrs:
          enabled: true
          negate: false

  oidc-hubble.yaml: |
    version: 1

    metadata:
      name: hubble OIDC

    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: hubble-oidc-provider
        state: present

        identifiers:
          name: hubble

        attrs:
          name: hubble
          client_id: hubble-envoy
          client_secret: !Env HUBBLE_OIDC_CLIENT_SECRET
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token

          authorization_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-authorization-implicit-consent]

          invalidation_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-invalidation-flow]

          signing_key: !Find
            - authentik_crypto.certificatekeypair
            - [name, authentik Self-signed Certificate]

          redirect_uris:
            - matching_mode: strict
              url: "https://hubble.${SECRET_DOMAIN}/oauth2/callback"

          property_mappings:
            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-openid]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-profile]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-email]

      - model: authentik_core.application
        id: hubble-application
        state: present

        identifiers:
          slug: hubble

        attrs:
          name: Hubble UI
          slug: hubble
          provider: !KeyOf hubble-oidc-provider
          meta_launch_url: "https://hubble.${SECRET_DOMAIN}/"
          meta_icon: "https://cdn.jsdelivr.net/gh/selfhst/icons/webp/cilium-hubble.webp"
          policy_engine_mode: any
          open_in_new_tab: true
          group: "Infrastructure / Network"
          meta_description: "Observability & Troubleshooting for Kubernetes Services"

      - model: authentik_policies.policybinding
        identifiers:
          target: !KeyOf hubble-application
          group: !Find [authentik_core.group, [name, infra-admins]]
          order: 0
        attrs:
          enabled: true
          negate: false

  oidc-prometheus.yaml: |
    version: 1

    metadata:
      name: prometheus OIDC

    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: prometheus-oidc-provider
        state: present

        identifiers:
          name: prometheus

        attrs:
          name: prometheus
          client_id: prometheus-envoy
          client_secret: !Env PROMETHEUS_OIDC_CLIENT_SECRET
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token

          authorization_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-authorization-implicit-consent]

          invalidation_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-invalidation-flow]

          signing_key: !Find
            - authentik_crypto.certificatekeypair
            - [name, authentik Self-signed Certificate]

          redirect_uris:
            - matching_mode: strict
              url: "https://prometheus.${SECRET_DOMAIN}/oauth2/callback"

          property_mappings:
            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-openid]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-profile]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-email]

      - model: authentik_core.application
        id: prometheus-application
        state: present

        identifiers:
          slug: prometheus

        attrs:
          name: Prometheus
          slug: prometheus
          provider: !KeyOf prometheus-oidc-provider
          meta_launch_url: "https://prometheus.${SECRET_DOMAIN}/"
          meta_icon: "https://github.com/prometheus/prometheus/raw/main/documentation/images/prometheus-logo.svg"
          policy_engine_mode: any
          open_in_new_tab: true
          group: "Infrastructure / Monitoring"
          meta_description: "The Prometheus monitoring system and time series database."

      - model: authentik_policies.policybinding
        identifiers:
          target: !KeyOf prometheus-application
          group: !Find [authentik_core.group, [name, infra-admins]]
          order: 0
        attrs:
          enabled: true
          negate: false

  oidc-alertmanager.yaml: |
    version: 1

    metadata:
      name: alertmanager OIDC

    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: alertmanager-oidc-provider
        state: present

        identifiers:
          name: alertmanager

        attrs:
          name: alertmanager
          client_id: alertmanager-envoy
          client_secret: !Env ALERTMANAGER_OIDC_CLIENT_SECRET
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token

          authorization_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-authorization-implicit-consent]

          invalidation_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-invalidation-flow]

          signing_key: !Find
            - authentik_crypto.certificatekeypair
            - [name, authentik Self-signed Certificate]

          redirect_uris:
            - matching_mode: strict
              url: "https://alertmanager.${SECRET_DOMAIN}/oauth2/callback"

          property_mappings:
            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-openid]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-profile]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-email]

      - model: authentik_core.application
        id: alertmanager-application
        state: present

        identifiers:
          slug: alertmanager

        attrs:
          name: Alertmanager
          slug: alertmanager
          provider: !KeyOf alertmanager-oidc-provider
          meta_launch_url: "https://alertmanager.${SECRET_DOMAIN}/"
          meta_icon: "https://github.com/prometheus/prometheus/raw/main/documentation/images/prometheus-logo.svg"
          policy_engine_mode: any
          open_in_new_tab: true
          group: "Infrastructure / Monitoring"
          meta_description: "The Alertmanager handles alerts sent by client applications such as the Prometheus server."

      - model: authentik_policies.policybinding
        identifiers:
          target: !KeyOf alertmanager-application
          group: !Find [authentik_core.group, [name, infra-admins]]
          order: 0
        attrs:
          enabled: true
          negate: false
  oidc-victorialogs.yaml: |
    version: 1

    metadata:
      name: victorialogs OIDC

    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: victorialogs-oidc-provider
        state: present

        identifiers:
          name: victorialogs

        attrs:
          name: victorialogs
          client_id: victorialogs-envoy
          client_secret: !Env VICTORIALOGS_OIDC_CLIENT_SECRET
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token

          authorization_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-authorization-implicit-consent]

          invalidation_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-invalidation-flow]

          signing_key: !Find
            - authentik_crypto.certificatekeypair
            - [name, authentik Self-signed Certificate]

          redirect_uris:
            - matching_mode: strict
              url: "https://victoria-logs.${SECRET_DOMAIN}/oauth2/callback"

          property_mappings:
            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-openid]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-profile]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-email]

      - model: authentik_core.application
        id: victorialogs-application
        state: present

        identifiers:
          slug: victorialogs

        attrs:
          name: Victoria Logs
          slug: victorialogs
          provider: !KeyOf victorialogs-oidc-provider
          meta_launch_url: "https://victoria-logs.${SECRET_DOMAIN}/"
          meta_icon: "https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/webp/victorialogs.webp"
          policy_engine_mode: any
          open_in_new_tab: true
          group: "Infrastructure / Monitoring"
          meta_description: "Fast and easy to use database for logs, which can efficiently handle terabytes of logs"

      - model: authentik_policies.policybinding
        identifiers:
          target: !KeyOf victorialogs-application
          group: !Find [authentik_core.group, [name, infra-admins]]
          order: 0
        attrs:
          enabled: true
          negate: false

  oidc-vmalert.yaml: |
    version: 1

    metadata:
      name: vmalert OIDC

    entries:
      - model: authentik_providers_oauth2.oauth2provider
        id: vmalert-oidc-provider
        state: present

        identifiers:
          name: vmalert

        attrs:
          name: vmalert
          client_id: vmalert-envoy
          client_secret: !Env VMALERT_OIDC_CLIENT_SECRET
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token

          authorization_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-authorization-implicit-consent]

          invalidation_flow: !Find
            - authentik_flows.flow
            - [slug, default-provider-invalidation-flow]

          signing_key: !Find
            - authentik_crypto.certificatekeypair
            - [name, authentik Self-signed Certificate]

          redirect_uris:
            - matching_mode: strict
              url: "https://vm-alerts.${SECRET_DOMAIN}/oauth2/callback"

          property_mappings:
            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-openid]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-profile]

            - !Find
              - authentik_providers_oauth2.scopemapping
              - [managed, goauthentik.io/providers/oauth2/scope-email]

      - model: authentik_core.application
        id: vmalert-application
        state: present

        identifiers:
          slug: vmalert

        attrs:
          name: Vmalert
          slug: vmalert
          provider: !KeyOf vmalert-oidc-provider
          meta_launch_url: "https://vm-alerts.${SECRET_DOMAIN}/"
          meta_icon: "https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/webp/victorialogs.webp"
          policy_engine_mode: any
          open_in_new_tab: true
          group: "Infrastructure / Monitoring"
          meta_description: "Vmalert is a rule-based alerting and recording service for VictoriaLogs. It evaluates rules and sends alerts to Alertmanager."

      - model: authentik_policies.policybinding
        identifiers:
          target: !KeyOf vmalert-application
          group: !Find [authentik_core.group, [name, infra-admins]]
          order: 0
        attrs:
          enabled: true
          negate: false